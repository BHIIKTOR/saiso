# Archived SAISO Materials

This directory stores legacy project materials that are not part of the active EVM/SVM v1.0 code path.

- `root/`: archived root-level QA, prompt, and historical planning markdown files.
- `docs/`: archived docs from previous SEI-era architecture.
- `archive/packages/`: archived inactive package code that is no longer in active workspaces.
